# JWT secret — must match the main app JWT_SECRET
JWT_SECRET=your-secret-key-change-in-production

# WebSocket server port
WORLD3D_PORT=4100

# TURN/STUN server (coturn) — for WebRTC voice chat
TURN_SECRET=generate-a-long-random-secret
TURN_REALM=localhost
# TURN_HOST=localhost
